server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location = /health {
    add_header content-type application/json;
    return 200 '{"service":"frontend","status":"ready"}';
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
