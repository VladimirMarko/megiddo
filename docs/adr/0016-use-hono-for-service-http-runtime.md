# Use Hono For Service HTTP Runtime

Backend services use Hono as the HTTP runtime around oRPC. Hono is lightweight, TypeScript-first, and easy to run as separate local processes, keeping the repository focused on service boundaries, contracts, and use cases rather than a heavy framework convention.
