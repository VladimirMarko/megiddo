# Use React Vite TanStack And Jotai For Frontend

The frontend uses React with Vite, TanStack Router for routing, TanStack Form for form state, and Jotai for client state. The todo UI can draw from the pmndrs Jotai todo examples as a reference, with license and attribution handled during implementation rather than copying unreviewed code blindly.
