# Version Published Contracts Append-Only

Published contract versions are append-only: stable Resource Schemas and oRPC procedures may be extended by adding fields, procedures, or new versions, but existing meanings are not mutated. Early unreleased examples may still change while the repository shape is being discovered; the append-only rule starts once a contract version is treated as published or stable.
